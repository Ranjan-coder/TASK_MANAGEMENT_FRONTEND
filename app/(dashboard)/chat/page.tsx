"use client";

// The layout.tsx handles sidebar + key setup + empty state.
// This file intentionally renders nothing — the layout handles everything.
export default function ChatPage() {
  return null;
}
